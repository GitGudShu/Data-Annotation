import { ImageIdParserPipe } from './image-id-parser.pipe';

describe('ImageIdParserPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageIdParserPipe();
    expect(pipe).toBeTruthy();
  });
});
